# 项目说明

> 本文件是对仓库根目录下两个子项目（`nutrition-tracker` 前端、`track-server` 后端）的整体梳理，基于实际代码与已有文档（开发文档 V3.0、前端数据存储文档、环境搭建文档）综合整理。代码现状优先于文档描述。

---

## 一、项目概述

**NutriTrack** 是一款每日热量与营养摄入记录工具，覆盖 PC 与移动端，支持账户体系与（规划中的）云端同步。

仓库为单体多项目结构：

```
calorie-track/
├── nutrition-tracker/   # 前端：React 18 + TS + Vite（移动优先）
├── track-server/        # 后端：Rust + Axum + PostgreSQL
├── 前端数据存储文档.md    # 前端本地存储（localStorage / IndexedDB）详细说明
├── 开发文档 V3.0.md       # 前后端需求与接口设计基线（注意：部分已与现状不一致）
├── 环境搭建文档.md        # 前端环境搭建指引
└── 项目说明.md           # 本文件
```

营养口径已统一精简为**四种核心营养素**：热量、碳水、蛋白质、脂肪。

---

## 二、前端 nutrition-tracker

### 1. 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18 | UI 框架 |
| TypeScript | 5.6 | 类型系统（严格模式） |
| Vite | 6 | 构建工具 |
| antd-mobile | 5 | 移动端 UI 组件库 |
| Zustand | 5 | 状态管理（含 persist 中间件） |
| React Router | 6 | 路由 |
| echarts | 6 | 图表 |
| idb | 8 | IndexedDB 离线存储 |
| axios | 1.7 | HTTP 客户端 |
| dayjs | 1.11 | 日期处理 |
| react-error-boundary | 6 | 错误边界 |

包管理器为 **npm**（见 [环境搭建文档.md](file:///e:/workspace/calorie-track/环境搭建文档.md)）。

### 2. 目录结构

```
nutrition-tracker/src/
├── api/              # API 请求封装（仅 auth + 注销账号）
├── assets/           # 图片资源
├── components/       # 通用组件（CSS Module），如 BottomNav、AddRecordPanel
├── data/             # 静态数据（系统食物等）
├── db/               # IndexedDB 数据层（db.ts / types.ts / 各 store）
├── pages/            # 页面组件
├── stores/           # Zustand 状态切片
├── styles/           # 全局样式
├── types/            # 类型定义
├── utils/            # 工具函数
├── App.tsx           # 根组件（路由 + 主题 + 自动登录）
└── main.tsx          # 入口文件
```

页面（`src/pages/`）实际包含：`Home`、`Stats`、`Profile`、`GoalSetting`（含 `DailyGoal`/`CycleGoal`）、`Foods`（`Create`/`Edit`/`CreatePlan`）、`Plans`（`EditPlan`）、`AddRecord`、`FoodLibrary`、`Login`、`Register`、`ForgotPassword`、`AddAccount`、`AccountInfo`、`Metabolism`、`Record`。

> 注意：实际页面比《开发文档 V3.0》路由表更丰富，已扩展出周期目标、套餐（Plan）、多账号、代谢计算等模块。

### 3. 路由（以 [App.tsx](file:///e:/workspace/calorie-track/nutrition-tracker/src/App.tsx) 为准）

| 路径 | 组件 | 说明 |
|------|------|------|
| `/` | Home | 首页看板 + 添加记录 |
| `/stats` | Stats | 统计页 |
| `/profile` | Profile | 个人中心 |
| `/goal-setting` | GoalSetting | 目标设置入口 |
| `/goal-setting/daily/:id?` | DailyGoal | 每日目标 |
| `/goal-setting/cycle/:id?` | CycleGoal | 周期目标（多日循环） |
| `/food-library` | FoodLibrary | 食物库 |
| `/record` | AddRecord | 添加记录 |
| `/foods/create-item` | CreateFood | 新建食物 |
| `/foods/edit/:id` | EditFood | 编辑食物 |
| `/foods/create-plan` | CreatePlan | 新建套餐 |
| `/plans/edit/:id` | EditPlan | 编辑套餐 |
| `/login` `/register` `/forgot-password` | … | 认证流程 |
| `/add-account` `/account-info` | … | 多账号管理 |
| `/metabolism` | Metabolism | 代谢计算 |

底部导航（`BottomNav`）仅在 `/`、`/stats`、`/profile` 三个主页面显示。`App.tsx` 内含 `AutoLogin`：从 `auth-storage` 读取 token 自动恢复会话。主题（`themeStore`）支持 `system`/`light`/`dark`，通过 `ConfigProvider` 注入 antd-mobile 配色。

### 4. 状态管理（Zustand stores）

见 [src/stores/](file:///e:/workspace/calorie-track/nutrition-tracker/src/stores) 与 [前端数据存储文档.md](file:///e:/workspace/calorie-track/前端数据存储文档.md)。

| Store | 存储键 | 职责 |
|-------|--------|------|
| `authStore` | `auth-storage` | 用户、token、登录态、本地账号、多账号切换 |
| `goalStore` | `goal-storage` | 目标模板（每日/周期）、`daily_goals` 数组、`today_index` |
| `foodStore` | `food-storage` | 食物库、使用次数、上次份量、跨账号保存关系 |
| `planStore` | `plan-storage` | 套餐（含完整食物对象）、使用次数、保存关系 |
| `recordStore` | `record-storage` | 饮食记录（含套餐记录、快速添加"不明项"） |
| `operationStore` | `operation-queue-storage` | 操作队列 + 同步序列号（按账号隔离） |
| `avatarStore` | — | 头像管理（IndexedDB 存储，不持久化到 localStorage） |
| `themeStore` | `theme-storage` | 主题模式 |

所有 store 通过 `persist` 中间件持久化到 localStorage，键名格式 `{name}-storage`。

### 5. 本地数据存储（双轨）

- **Zustand + localStorage**：认证、目标、食物、套餐、记录、操作队列、主题（小数据、同步读写）。
- **IndexedDB**（`idb`，库名 `CalorieTrackerDB` v2）：`records` / `foods` / `goals` / `preferences` / `avatars` 五张表，作为大数据的异步持久化备份。详见 [前端数据存储文档.md](file:///e:/workspace/calorie-track/前端数据存储文档.md) 第三章。

### 6. 关键设计

- **本地优先（Local-first）**：所有业务数据（食物/套餐/记录/目标）默认存本地，未登录时使用固定本地账号 `local-account`，开箱即用。
- **多账号隔离**：数据按 `currentAccountId` 隔离（目标模板、操作队列按账号 ID 分桶；食物/套餐通过 `user_id` 归属 + `savedFoodIds`/`savedPlanIds` 记录保存关系）。
- **跨账号编辑规则**：编辑归属他人的食物/套餐时，生成新 ID、归属改为当前账号，原项保留。
- **操作队列与同步序列号**：增删改写入 `operationStore` 队列，`syncSerialNumber` 记录已同步位置，用于增量同步设计（见下文"同步现状"）。
- **目标模型统一**：每日目标 `cycle_days=1`，周期目标 `cycle_days=N`，统一用 `daily_goals` 数组存储；`today_index` 作为计算当日目标的锚点；`type` 仅用于界面区分。
- **套餐内嵌完整食物对象**：`LocalPlanItem` 含完整 `food`，原食物删除后套餐仍可计算营养。

### 7. API 通信

见 [src/api/index.ts](file:///e:/workspace/calorie-track/nutrition-tracker/src/api/index.ts)。

- axios 实例 baseURL 为 `https://track-server.fishfree.fun`（生产环境地址）。
- 请求拦截器注入 `Authorization: Bearer <token>`。
- 响应拦截器：401 时若非本地账号且不在登录/注册页，则登出并跳转 `/login`。
- API 模块封装：
  - `authAPI`：注册/登录/忘记密码/重置密码/修改密码
  - `userAPI`：获取用户信息/注销账号
  - `recordsAPI`：按日期/日期范围获取记录
  - `foodsAPI`：获取食物列表
  - `goalTemplatesAPI`：目标模板 CRUD + 设为当前
  - `syncAPI`：版本化同步 + 头像上传/检查/获取
  - `systemAPI`：健康检查
- 所有服务器数据请求**不阻塞界面**，错误使用 `console.error` 静默处理。

### 8. 构建/开发

```bash
cd nutrition-tracker
npm install
npm run dev        # http://localhost:5173，host:true 支持手机真机调试
npm run build      # tsc -b && vite build → dist/
npm run lint
```

Vite 配置（[vite.config.ts](file:///e:/workspace/calorie-track/nutrition-tracker/vite.config.ts)）：端口 5173；`/api` 代理到 `http://localhost:3000`（移除前缀）；`allowedHosts` 含 `nutrition-tracker.fishfree.fun`；构建未启用压缩与 sourcemap。

---

## 三、后端 track-server

### 1. 技术栈

- **语言/框架**：Rust（edition 2021）+ Axum 0.7 + Tokio
- **数据库**：PostgreSQL + SQLx 0.7（异步、含 migrate、postgres、uuid、chrono 特性）
- **认证**：JWT（`jsonwebtoken` 9）+ bcrypt 0.15
- **HTTP 中间件**：tower / tower-http（CORS、trace）
- **其他**：serde、uuid、chrono、anyhow、thiserror、validator、tracing、dotenv

### 2. 目录结构

```
track-server/src/
├── main.rs              # 入口：路由组装、管理员初始化、schema 自愈
├── lib.rs               # 模块导出
├── auth.rs              # JWT 签发与校验
├── database.rs          # PgPool 创建
├── error.rs             # 统一错误类型
├── middleware.rs        # auth_middleware（Bearer Token 解析注入 user_id）
├── models.rs            # 请求/响应/ORM 模型
└── handlers/            # 各资源处理器（分层：handler → sqlx 查询）
    ├── auth.rs          # 注册/登录
    ├── user.rs          # 用户信息、偏好、注销
    ├── foods.rs         # 食物 CRUD
    ├── meal_records.rs  # 记录 CRUD
    ├── records_batch.rs # 记录批量创建
    ├── goals.rs         # 用户目标（含周期 daily_goals）
    ├── plans.rs         # 套餐 CRUD
    ├── favorites.rs     # 收藏
    ├── stats.rs         # 每日/周期统计
    └── admin.rs         # 系统食物管理（管理员）
migrations/              # SQL 迁移（001_initial，整合所有表结构）
```

### 3. 路由（以 [main.rs](file:///e:/workspace/calorie-track/track-server/src/main.rs) 为准）

> ⚠️ [track-server/README.md](file:///e:/workspace/calorie-track/track-server/README.md) 中的路由（如 `/api/meal-records`、`/api/goals`、`/api/favorites/{id}`）**已过时**，实际以 `main.rs` 为准。

**公开路由**
- `GET /health`
- `POST /api/auth/register`、`POST /api/auth/login`

**受保护路由**（经 `auth_middleware`）
- 用户：`GET|PUT|DELETE /api/user/me`、`GET|PUT /api/user/goals`、`GET|PUT /api/user/preferences`、`GET|POST /api/user/favorites`、`DELETE /api/user/favorites/:food_id`
- 头像：`GET /api/avatars/:hash`、`POST /api/avatars`、`GET /api/avatars/check/:hash`
- 食物：`GET|POST /api/foods`、`GET|PUT|DELETE /api/foods/:id`
- 记录：`GET|POST /api/records`、`GET|PUT|DELETE /api/records/:id`、`POST /api/records/batch`
- 套餐：`GET|POST /api/plans`、`GET|PUT|DELETE /api/plans/:id`
- 统计：`GET /api/stats/daily`、`GET /api/stats/nutrients`

**管理员路由**（同样经 `auth_middleware`，需 `is_admin=true`）
- `GET /api/admin/foods`、`PUT|DELETE /api/admin/foods/:id`

CORS 全开放（`allow_origin(Any)`）。默认监听 `0.0.0.0:3000`（可由 `HOST`/`PORT` 环境变量覆盖）。

### 4. 数据库模型（关键表）

迁移演进要点（见 [migrations/](file:///e:/workspace/calorie-track/track-server/migrations)）：

- `users`：含 `is_admin` 字段、`avatar_hash`（关联 `avatars.hash`）
- `avatars`：头像表，`hash`（SHA-256 唯一索引）+ `data`（base64）
- `foods`：含 `unit`、`is_system`、`user_id`（NULL 表示系统食物）
- `user_goals`：经多次演进，最终保留 `user_id`、`daily_goals`（JSONB，含周期循环数组）、`operation_count`、`updated_at`（早期 `calorie_target`/`carb_target_g` 等冗余列已在 `0009` 清理）
- `meal_records`：`user_id`、`food_id`、`serving_count`、`record_time`
- `user_favorite_foods`：联合主键 `(user_id, food_id)` + `sort_order`
- `user_food_usage`：`(user_id, food_id)` + `usage_count`（添加记录时 `INSERT ON CONFLICT DO UPDATE` 递增）
- `user_preferences`：`theme`、`dietary_preference`、`language`
- `meal_plans` / `plan_items`：套餐及套餐内食物项（`main.rs` 启动时 `CREATE TABLE IF NOT EXISTS` 自愈）
- `operation_logs`：操作日志表，支持版本化同步

### 5. 认证与中间件

- JWT 密钥来自 `JWT_SECRET` 环境变量（未设置时回退到默认值，**生产必须覆盖**）。
- `auth_middleware` 校验 Bearer Token，解析 `sub` 为 `user_id` 注入请求扩展。
- 密码 bcrypt 哈希存储。

### 6. 管理员机制

[main.rs](file:///e:/workspace/calorie-track/track-server/src/main.rs) 启动时执行 `init_admin_user`：确保邮箱 `804266055@qq.com` 的用户存在且 `is_admin=true`；不存在则用 `ADMIN_INITIAL_PASSWORD`（未设置则随机生成并打印日志）创建。`init_database_schema` 会对 `user_goals.daily_goals` 列及 `meal_plans`/`plan_items` 表做幂等补建。

### 7. 构建/运行

```bash
cd track-server
cp .env.example .env          # 配置 DATABASE_URL / JWT_SECRET / HOST / PORT
# PostgreSQL 12+，推荐 Docker：
docker run --name calorie-track-db -e POSTGRES_DB=calorie_track \
  -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres:15
cargo install sqlx-cli && sqlx database create && sqlx migrate run
cargo run                     # http://localhost:3000
cargo test / cargo clippy / cargo fmt
```

环境变量（`.env`）：`DATABASE_URL`、`JWT_SECRET`、`HOST`、`PORT`、`ADMIN_INITIAL_PASSWORD`（可选）。

---

## 四、前后端协作与现状

### 1. 连接方式

前端 axios baseURL 为 `https://track-server.fishfree.fun`（生产环境地址），直接请求后端 API，不经过 Vite proxy。

### 2. 数据同步现状（重要）

- **版本化同步已实现**：前端 `operationStore` 通过 `syncAPI.versionedSync()` 实现版本化增量同步。
- **同步流程**：
  1. 用户操作（增删改）写入 `operationStore` 队列，操作 ID 使用 UUID v4（`crypto.randomUUID()`）
  2. 每秒轮询 `performSync()`，发送队头操作到 `/api/sync/versioned`
  3. 后端通过操作 ID 查重（`operation_logs.id`），已存在则返回 `head_processed=true`
  4. 后端异步处理队列，写入业务表 + `operation_logs`（使用前端提供的操作 ID 作为主键）
  5. 前端收到 `head_processed=true` 后移除队头操作，更新 `data_version`
- **头像同步**：写入/查询/修改 IndexedDB 头像时，并发检查服务器是否已有该哈希，不存在则上传
- **本地账号**（`local-account`）跳过所有服务器请求，避免 401 错误

### 3. 文档与代码差异提示

| 项 | 文档 | 实际 |
|----|------|------|
| 前端页面 | 路由表 7 项 | 已扩展至 15+ 项（套餐、周期目标、多账号、代谢等） |
| 前端 API | 仅 auth + 注销账号 | 已封装 auth/user/records/foods/goals/sync/avatars/system 多模块 |
| 后端路由 | README 列 `/api/meal-records` 等 | `main.rs` 实为 `/api/records`、`/api/user/goals` 等 |
| 目标模型 | 单日目标 | 已升级为 `daily_goals` JSONB 周期目标 |
| 数据同步 | 待接线 | 已实现版本化同步（versionedSync） |
| 头像存储 | user.avatar_url | 已改为 user.avatar_hash，头像数据独立存储在 avatars 表 |

排查问题时**以代码为准**，文档作为背景与设计意图参考。

---

## 五、相关文档索引

- [前端数据存储文档.md](file:///e:/workspace/calorie-track/前端数据存储文档.md) — localStorage / IndexedDB 字段级说明、账号隔离规则、数据流
- [开发文档 V3.0.md](file:///e:/workspace/calorie-track/开发文档%20V3.0.md) — 前后端需求基线与接口设计（部分已过时）
- [环境搭建文档.md](file:///e:/workspace/calorie-track/环境搭建文档.md) — 前端 Node/npm 环境与目录结构
- [track-server/README.md](file:///e:/workspace/calorie-track/track-server/README.md) — 后端运行指引（路由部分已过时，以 main.rs 为准）

---

## 六、开发须知

1. **以后端 `main.rs` 为路由唯一真相**，README 路由表请勿直接引用。
2. **前端 axios baseURL** 为 `https://track-server.fishfree.fun`，如需切换环境请修改 [api/index.ts](file:///e:/workspace/calorie-track/nutrition-tracker/src/api/index.ts)。
3. **本地账号 `local-account`** 是默认体验入口，所有数据隔离逻辑都需兼容该 ID。
4. **跨账号编辑食物/套餐会生成新 ID**，调试数据归属时需留意。
5. **操作 ID 使用 UUID v4**（`crypto.randomUUID()`），用于后端去重和 `operation_logs.id` 主键，必须符合 PostgreSQL UUID 格式。
6. **头像同步触发时机**：IndexedDB 写入/查询/修改头像数据时，并发检查服务器是否已有该哈希，不存在则上传。
7. **注册时不生成默认目标**：无目标时前端显示默认值（2000kcal/250g碳水/150g蛋白质/65g脂肪），用户可手动添加。
8. **生产环境**务必设置强 `JWT_SECRET`、收紧 CORS、启用 HTTPS。
