# 环境搭建文档

## 1. 系统要求

| 项目      | 要求                         |
| ------- | -------------------------- |
| Node.js | **20 LTS 或更高版本**（推荐 22+）   |
| 包管理器    | **npm**                    |
| 操作系统    | Windows / macOS / Linux 均可 |

## 2. 安装 Node.js

### Windows

1. 访问 [Node.js 官网](https://nodejs.org/) 下载 LTS 版本安装包
2. 运行安装程序，保持默认选项即可
3. 验证安装：

```bash
node -v   # 应输出 v20.x.x 或更高
npm -v    # 应输出 10.x.x 或更高
```

### 使用 nvm 管理版本（推荐）

```bash
# 安装 nvm-windows: https://github.com/coreybutler/nvm-windows/releases
nvm install 22
nvm use 22
```

## 3. 项目初始化

### 3.1 进入前端目录

```bash
cd nutrition-tracker
```

### 3.2 安装依赖

```bash
npm install
```

### 3.3 启动开发服务器

```bash
npm run dev
```

开发服务器启动后：

- 访问地址：`http://localhost:5173`
- 局域网访问：`http://<本机IP>:5173`（已开启 `host: true`，支持手机真机调试）

### 3.4 构建生产版本

```bash
npm run build
```

构建产物输出到 `dist/` 目录。

### 3.5 预览构建产物

```bash
npm run preview
```

### 3.6 代码检查

```bash
npm run lint
```

## 4. 后端服务依赖

### 4.1 JWT 密钥配置

后端启动时必须设置 `JWT_SECRET`，且长度至少为 32 字节。可使用以下命令生成安全随机密钥：

```bash
openssl rand -base64 32
```

将生成的值写入 `track-server/.env`：

```env
JWT_SECRET=生成的至少32字节密钥
```

开发环境下，前端通过 Vite proxy 将 `/api/*` 请求代理到后端服务：

| 配置项  | 值                       |
| ---- | ----------------------- |
| 代理路径 | `/api`                  |
| 目标地址 | `http://localhost:3000` |
| 路径重写 | 移除 `/api` 前缀            |

**启动前端前，请确保后端服务已运行在 3000 端口。**

后端项目位于 `track-server/` 目录，详见后端文档。

## 5. 技术栈概览

| 技术           | 版本   | 说明                |
| ------------ | ---- | ----------------- |
| React        | 18   | UI 框架             |
| TypeScript   | 5.6  | 类型系统（严格模式）        |
| Vite         | 6    | 构建工具              |
| antd-mobile  | 5    | 移动端 UI 组件库        |
| Zustand      | 5    | 状态管理              |
| React Router | 6    | 路由管理              |
| echarts      | 6    | 图表库               |
| idb          | 8    | IndexedDB 离线存储    |
| dayjs        | 1.11 | 日期处理              |
| ESLint       | 10   | 代码检查（Flat Config） |

## 6. 项目目录结构

```
nutrition-tracker/
├── public/                      # 静态资源
├── src/
│   ├── api/                     # API 请求封装
│   ├── assets/                  # 图片资源
│   ├── components/              # 通用组件（CSS Module）
│   ├── data/                    # 静态数据
│   ├── db/                      # IndexedDB 数据层
│   ├── pages/                   # 页面组件
│   │   ├── AddAccount/          # 添加账号
│   │   ├── AddRecord/           # 添加记录
│   │   ├── FoodLibrary/         # 食物库
│   │   ├── Foods/               # 食物创建/编辑/套餐
│   │   ├── GoalSetting/         # 目标设置
│   │   ├── Home/                # 首页（今日记录）
│   │   ├── Login/               # 登录
│   │   ├── Profile/             # 个人中心
│   │   ├── Register/            # 注册
│   │   └── Stats/               # 统计
│   ├── stores/                  # Zustand 状态管理
│   ├── styles/                  # 全局样式
│   ├── types/                   # 类型定义
│   ├── utils/                   # 工具函数
│   ├── App.tsx                  # 根组件（路由 + 主题）
│   └── main.tsx                 # 入口文件
├── vite.config.ts               # Vite 配置
├── tsconfig.json                # TypeScript 配置
├── eslint.config.js             # ESLint 配置
└── package.json
```

## 7. 关键配置说明

### Vite 配置（vite.config.ts）

- 开发端口：`5173`
- API 代理：`/api` → `http://localhost:3000`（移除前缀）
- 构建产物：`dist/`，未启用压缩和 sourcemap

### TypeScript 配置

- 采用项目引用模式（`tsconfig.json` → `tsconfig.app.json` + `tsconfig.node.json`）
- 严格模式：`noUnusedLocals`、`noUnusedParameters` 均开启
- 模块解析：`bundler`
- 仅类型检查，不输出 JS（`noEmit: true`）

### ESLint 配置

- 使用 Flat Config 格式
- 启用 React Hooks 规则和 React Refresh 规则
- 全局忽略 `dist` 目录

## 8. 常见问题

### Q: 启动后 API 请求报错

确保后端服务已运行在 `localhost:3000`。检查 Vite 控制台是否有代理错误日志。

### Q: 手机访问开发服务器

开发服务器已开启 `host: true`，手机和电脑需在同一局域网。访问 `http://<电脑IP>:5173`。

### Q: npm install 失败

尝试清除缓存：

```bash
npm cache clean --force
rm -rf node_modules
npm install
```

### Q: 构建时 TypeScript 报错

```bash
npm run lint       # 检查代码规范
npx tsc -b         # 单独运行类型检查
```

---

## 9. Tauri Android APK 构建

`track-client/` 是 Tauri v2 项目，复用 `nutrition-tracker/` 的前端界面，构建目标为 Android APK。

### 9.1 JDK 17 安装

```powershell
winget install Microsoft.OpenJDK.17
```

安装后设置环境变量（以默认安装路径为例）：

```powershell
[System.Environment]::SetEnvironmentVariable("JAVA_HOME", "C:\Program Files\Microsoft\jdk-17.0.x.x-hotspot", "User")
[System.Environment]::SetEnvironmentVariable("PATH", "$env:PATH;%JAVA_HOME%\bin", "User")
```

验证：

```powershell
java -version
# 应输出 openjdk version "17.x.x"
```

### 9.2 Android SDK 安装

从 Android 官方下载 [commandlinetools-win](https://developer.android.com/studio#command-line-tools-only)，解压到固定目录（如 `C:\Android\sdk\cmdline-tools\latest`）。

目录结构：

```
C:\Android\sdk\
└── cmdline-tools\
    └── latest\
        ├── bin\
        ├── lib\
        └── source.properties
```

设置环境变量：

```powershell
[System.Environment]::SetEnvironmentVariable("ANDROID_HOME", "C:\Program Files\Android\sdk", "User")
[System.Environment]::SetEnvironmentVariable("ANDROID_SDK_ROOT", "C:\Program Files\Android\sdk", "User")
[System.Environment]::SetEnvironmentVariable("PATH", "$env:PATH;C:\Program Files\Android\sdk\cmdline-tools\latest\bin;C:\Program Files\Android\sdk\platform-tools", "User")
```

安装 `android` CLI 工具（用于管理 SDK 包）：

```powershell
curl -fsSL https://dl.google.com/android/cli/latest/windows_x86_64/install.cmd -o "%TEMP%\android-cli-install.cmd" && "%TEMP%\android-cli-install.cmd"
```

验证：

```powershell
android --version
```

安装 SDK 组件（使用最新稳定版）：

```powershell
android sdk install platform-tools platforms/android-37 build-tools/37.0.0
```

> `android sdk install` 会自动处理许可证接受，无需手动执行 `sdkmanager --licenses`。如需查看所有可用包，运行 `android sdk list --all`。

### 9.3 Android NDK 安装

```powershell
android sdk install ndk/29.0.14206865
```

设置 NDK 环境变量：

```powershell
[System.Environment]::SetEnvironmentVariable("NDK_HOME", "C:\Android\sdk\ndk\29.0.14206865", "User")
```

### 9.4 Rust Android 编译目标

```powershell
rustup target add aarch64-linux-android armv7-linux-androideabi i686-linux-android x86_64-linux-android
```

验证：

```powershell
rustup target list --installed
# 应包含上述四个 android 目标
```

### 9.5 安装 Tauri CLI

Tauri CLI 通过 cargo 全局安装：

```powershell
cargo install tauri-cli --version "^2"
```

验证：

```powershell
cargo tauri --version
```

### 9.6 生成应用图标

准备一张 1024x1024 的 PNG 图标，放置到 `nutrition-tracker/public/app-icon.png`，然后执行：

```powershell
cd track-client
cargo tauri icon ../nutrition-tracker/public/app-icon.png
```

这将在 `src-tauri/icons/` 目录下生成所有尺寸的图标。

### 9.7 初始化 Android 项目

```powershell
cd track-client
cargo tauri android init
```

此命令会在 `src-tauri/gen/android/` 目录下生成 Android 项目文件。

### 9.8 构建 APK

```powershell
cd track-client
cargo tauri android build --apk
```

开发调试模式：

```powershell
cd track-client
cargo tauri android dev
```

输出路径：`src-tauri/gen/android/app/build/outputs/apk/universal/release/app-universal-release-unsigned.apk`

### 9.9 环境变量汇总

完成所有安装后，确保以下环境变量已设置：

| 变量                 | 值                                                 |
| ------------------ | ------------------------------------------------- |
| `JAVA_HOME`        | `C:\Program Files\Microsoft\jdk-17.0.x.x-hotspot` |
| `ANDROID_HOME`     | `C:\Program Files\Android\sdk`                    |
| `ANDROID_SDK_ROOT` | `C:\Program Files\Android\sdk`                    |
| `NDK_HOME`         | `C:\Android\sdk\ndk\29.0.14206865`                |

---

## 10. Tauri 构建常见问题

### Q: 构建时报 "No JDK found"

确保 `JAVA_HOME` 指向 JDK 17+ 目录，且 `java -version` 能正常执行。

### Q: 构建时报 "NDK not found"

确保 `NDK_HOME` 环境变量正确指向 NDK 目录。可通过 `echo $env:NDK_HOME` 检查。

### Q: 构建时报 "Android SDK not found"

确保 `ANDROID_HOME` 和 `ANDROID_SDK_ROOT` 正确设置。可通过 `android sdk list --all` 验证。

### Q: Rust 编译失败

确保已安装所有 Android 编译目标：`rustup target list --installed`。
